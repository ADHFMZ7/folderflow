[project]
name = "folderflow-engine"
version = "0.1.0"
description = "The FolderFlow engine: watches folders and runs agents on new files"
readme = "README.md"
authors = [
    { name = "adhfmz7", email = "ahmadaldasouqi559@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
folderflow-engine = "folderflow_engine:main"

[build-system]
requires = ["uv_build>=0.8.17,<0.9.0"]
build-backend = "uv_build"
